---
'awaitly-analyze': patch
---

Improve CLI defaults by auto-selecting the most useful diagram type when no explicit format is provided.

Add workflow-aware auto-detection between railway and mermaid output, expose the new diagram inference helper, and add a `--watch` mode for live re-analysis during editing.
